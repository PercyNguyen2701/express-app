<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <title>Hình chữ nhật</title>
    <style>
      body {
        font-family: sans-serif;
        padding: 2rem;
      }

      form {
        margin-bottom: 2rem;
      }

      input {
        margin: 0.5rem 0;
        padding: 0.5rem;
        width: 100%;
        max-width: 300px;
      }

      button {
        padding: 0.5rem 1rem;
      }

      .result {
        background: #f0f0f0;
        padding: 1rem;
        max-width: 300px;
        border-radius: 5px;
      }
    </style>
  </head>
  <body>
    <h1>Thông tin Hình chữ nhật</h1>

    <!-- Sử dụng POST method -->
    <form method="POST" action="/rect">
      <label for="width">Chiều rộng:</label><br />
      <input type="number" id="width" name="width" value="<%= width || '' %>" required /><br />

      <label for="height">Chiều cao:</label><br />
      <input type="number" id="height" name="height" value="<%= height || '' %>" required /><br />

      <button type="submit">Tính toán</button>
    </form>

    <% if (perimeter && area) { %>
    <div class="result">
      <h3>Kết quả:</h3>
      <p>Chiều rộng: <%= width %></p>
      <p>Chiều cao: <%= height %></p>
      <p>Chu vi: <%= perimeter %></p>
      <p>Diện tích: <%= area %></p>
    </div>
    <% } %>
  </body>
</html>
